<% if !@groups.empty? %>
<% @groups.each do |group| %>
    -<%= button_to "#{group.name}", "/groups/#{group.id}", :method => :post %><br> 
    <%= group.description %><br><br>
    <% end %>
      <%= button_to "Would you like to create a group", "/groups/new", :method => :get %>
<% elsif @groups.empty? %>
    No available groups
    <%= button_to "Would you like to create a group", "/groups/new", :method => :get %>
    <% end %>